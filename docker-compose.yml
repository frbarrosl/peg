services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: peg
      POSTGRES_USER: peg
      POSTGRES_PASSWORD: peg
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
